# 🌙 Nightly AI Report — 2026-04-26

> Generated by NightlyJobs AI Agent at ~02:00 UTC

---

## Security Findings

### New findings this run

| Severity | Finding | Location |
|---|---|---|
| 🔴 High | `PentestSuiteTest` is `@Disabled` — entire spec-driven suite not running in CI | `PentestSuiteTest.java:36` |
| 🟡 Medium | Empty-sessionId bypass test missing for `getPaymentStatus` and `deleteAccount` | `AuthTestTemplates.java:88` |
| 🟡 Medium | No IDOR probe for `deleteAccount.accountId` ownership | `AccessControlTestTemplates.java:139` |
| 🟡 Medium | `@Tag("pentest")` on `PentestSuiteTest` should be `@Tag("spec-driven")` | `PentestSuiteTest.java:37` |
| 🟢 Low | `config/pentest.properties` missing — all probe params use hardcoded defaults | `config/` directory |

### Previously known findings (from CLAUDE.md — tests exist)
- IDOR on `/payment/status/{id}` — `payment_api.go:1043` — **tested** ✅
- IDOR on `/payment/summary/{id}` — **tested** ✅
- Cross-user session vulnerability — **tested** ✅
- Empty sessionId bypass (`payment_api.go:222`) — **partially tested** (getPaymentTypes only) ⚠️
- CORS wildcard, missing HSTS, missing X-Content-Type-Options — **tested** ✅
- SSRF via Stripe webhook — **tested** (intentional fail) ✅

---

## Dependency CVEs

| Library | Issue | Action |
|---|---|---|
| nimbus-jose-jwt 9.37.3 | Transitive json-smart CVE-2023-1370 (HIGH) may apply — depends on resolved json-smart version | Run `mvn dependency:tree \| grep json-smart`; upgrade if ≤ 2.4.10 |
| All others | No HIGH/CRITICAL CVEs found | No action needed |

**Build environment note:** `pom.xml` requires Java 24 but CI runner has Java 21 (`JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64`). Confirm nightly workflow installs JDK 24 or downgrade the compiler target.

---

## PR Reviews

| PR | Title | Verdict | Key issues |
|---|---|---|---|
| [#24](https://github.com/neha-eglund/claude-github-demo/pull/24) | Add getInt to ConfigLoader | ✅ **APPROVE** | — |
| [#29](https://github.com/neha-eglund/claude-github-demo/pull/29) | Add SeverityClassifier | ❌ **REQUEST_CHANGES** | `System.out.println`; class not `final`/private-ctor |
| [#76](https://github.com/neha-eglund/claude-github-demo/pull/76) | TestConfig + ownership test | ❌ **REQUEST_CHANGES** | Credential-shaped hardcoded fallback; test validates vulnerable behavior |
| [#77](https://github.com/neha-eglund/claude-github-demo/pull/77) | PaymentFinding class | ❌ **REQUEST_CHANGES** | `System.out.println`; mutable POJO (should be record) |
| [#78](https://github.com/neha-eglund/claude-github-demo/pull/78) | Issue creation + URL consolidation | ❌ **REQUEST_CHANGES** | `PENTEST_CREATE_ISSUES=true` in PR workflow (blocked by policy); Stripe URL replaced with Payment API secret |

---

## Recommended Actions — Priority Order

1. **[TODAY — CI broken]** Re-enable `PentestSuiteTest` — change `@Disabled` → remove, `@Tag("pentest")` → `@Tag("spec-driven")`. Without this, the nightly pentest produces zero Payment API coverage.

2. **[TODAY — security]** Reject PR #78: `PENTEST_CREATE_ISSUES=true` in `claude-code-review.yml` will spam GitHub issues on every PR push; Stripe URL change will silently break all Stripe tests.

3. **[TODAY — security]** Reject PR #76: the ownership test is a false positive (passes when IDOR exists). Requires rewrite with correct assertion (`statusCode == 403`) and actual cross-user userId in request params.

4. **[THIS WEEK — security]** Add empty-sessionId bypass probes to `getPaymentStatus` and `deleteAccount` in `AuthTestTemplates`.

5. **[THIS WEEK — security]** Add IDOR probe for `deleteAccount` accountId ownership in `AccessControlTestTemplates`.

6. **[THIS WEEK — maintenance]** Verify `mvn dependency:tree | grep json-smart` — upgrade nimbus-jose-jwt if json-smart ≤ 2.4.10 is resolved.

7. **[THIS WEEK — maintenance]** Fix PRs #29 and #77 for `System.out.println` → SLF4J and utility-class structure.

---

## All Clear

No new HIGH/CRITICAL CVEs confirmed in direct dependencies (logback 1.4.14 and nimbus-jose-jwt 9.37.3 are both patched for their respective known CVEs). The transitive json-smart risk requires a one-command verification before declaring full clearance.

---

*Report generated by NightlyJobs AI Agent — branch `claude/admiring-hypatia-dSYOr`*
*Note: GitHub MCP server was unavailable (authorization error); issues and PR reviews were recorded in this file rather than posted directly to GitHub. Re-run after GitHub Copilot authorization is established to enable live GitHub interactions.*
